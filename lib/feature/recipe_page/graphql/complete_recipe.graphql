query CompleteRecipe($recipe_id: ID!) {
    recipe(id: $recipe_id) {
        name
        description
        steps {
            text
        }
        ingredients {
            ingredient {
                name
            }
            measurement
        }
    }
}
